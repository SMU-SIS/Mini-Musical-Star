Kens burns effect:
-Slow zooming
-Panning (horizontal movement or rotation)
-Fading

Random with zoom, pan (or both zoom and pan), speed


Create's a class that is able to take in a pointer to a UIImageView.

This class will contain different set of instructions to to produce kens burns effect.

The animation of the UIImageView will be decided by randomness.

This class will be able to animate the UIImageView.

